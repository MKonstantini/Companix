// CRM Cards Layout:
// Accessible for Admin
// Displays all users in DB (table)
// Can deleted and change user's account types

import { FunctionComponent } from "react";

interface CRMCardsProps {

}

const CRMCards: FunctionComponent<CRMCardsProps> = () => {
    return (
        <>
        </>
    );
}

export default CRMCards;